/*
JDBC -> ORM(HiberNate) -> FrameWork(Spring Data) - Посока на вдигане на абстракцията.
С вдигането на абстракцията се намалява контрола върху това как точно работи програмата, 
но се улеснява разработването.
Когато проект е по-прост може да се използва по-ниска абстракция.

JPA -> Java Persistens API е съвкупност от правила(Докумнетация, Стандарт, Спецификация), които описват как да се имплементира връзката ме жду базата и програмата(клиента). 
(Примери за имплементация са HiberNate и EclipseLink.)
Този стандарт позволява имплементацията да се преизползва от всички.
Позволява и лесно да преминаваш от един ORM към друг.

При Update и Delete трябва да използваме отваряне и затваряне на транзакции(getTransaction().begin{}getTransaction.commit)


 





*/